import React from 'react';
import { Typography } from '@mui/material';

const CartProtductRemove = ({ onClick }) => (
  <Typography onClick={onClick}>CartProtductRemove</Typography>
);

export default CartProtductRemove;
